# SPEC.md — oto-utsushi(音写ラボ)

青空文庫の一節を**声に出して読み**、ブラウザ内で動く音声認識の出力と原文を突き合わせて、
**機械が聞き落とす場所**を地図にする。推論はすべてクライアントで完結し、
音声もテキストもネットワークへ出ない(実行時課金ゼロ)。

## 1. 目的

音声認識アプリの多くは「認識結果」を出して終わる。その結果が正しいかは誰も知らない。
このアプリは順序を逆にする — **正解(原文)が先に画面に在り**、ユーザはそれを読み上げる。
だから認識の誤りを厳密に測れる。

測るのは人の滑舌ではなく**機械の側の癖**である。固有名詞・文語・数字・振り仮名を要する語で
認識がどう崩れるかを、原文上の位置に貼って見せる。採点ではなく計量。

この設計の要は **参照テキストを認識器へ一切渡さないこと**(N-06 / G-03)。
Whisper には `initial_prompt` に文脈を与えて精度を上げる正当な使い方があるが、
ここでそれをやると自分の答案を採点に混ぜることになり、測定値が無意味になる。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 課題文カタログの選定パイプライン。`aozora-sakuin/data/normalized`(青空文庫 5,000 作の正規化済み本文)から、**新字新仮名**かつ音読可能な一節を機械的に抽出し、出典(作品 ID・題名・著者・本文オフセット)とともに固定する | must |
| F-02 | テキスト正規化器。比較のための写像を与える純関数。**原文位置を失わない** — 正規化後の各文字が原文のどのインデックス由来かを保持する | must |
| F-03 | アライメント器。文字単位の Levenshtein DP で参照と認識結果を整列し、編集操作列(一致/置換/挿入/削除)を復元して**原文位置へ逆写像**する | must |
| F-04 | 計量器。厳密 CER・表記ゆれ許容 CER・操作内訳・読み飛ばし(連続削除 ≥ k)・言い直し(挿入の直後に同一部分列が再開)・読速(文字/秒) | must |
| F-05 | 音声前処理。任意のサンプルレートのモノラル/ステレオを 16 kHz モノラル `Float32Array` に変換する純関数(帯域制限つき) | must |
| F-06 | 表記ゆれ辞書。カタカナ/ひらがな・繰り返し記号の展開・旧字/新字・数字表記など、**決定論的に同一の読みを主張できる**変換のみ。出所をエントリごとに記し、**網羅でない表はそう明示する** | must |
| F-07 | 認識エンジン境界。`Recognizer` インタフェースを定義し、実装(loop_002 の Whisper / Web Speech)を差し替え可能にする。**インタフェースの入力に参照テキストを表す型を含めない** | must |
| F-08 | フリート共通フッタ(License・GitHub・歩き方/設計図・App Menu) | must |

`F-07` の実装本体(transformers.js + WebGPU での Whisper 実行)と録音 UI は loop_002 で入れた。

**推論そのものは実ブラウザでしか確かめられない。** loop_003 で `npm run browser` を用意した —
静的書き出しをそのまま配って Chromium で開き、日本語 TTS の WAV を疑似マイクへ流して
**マイク → 録音 → リサンプル → Whisper → 計量**の全経路を通す。

そこで **loop_002 の緑をすり抜けていた故障**が出た(次節)。

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。API Route / サーバ処理 / cron を持たない | `next build` が `out/` を生成し、`app/api` が存在しない |
| N-02 | `src/core` は純関数のみ。React/DOM/Node API を import しない。カバレッジ lines/functions/statements ≥ 90%、branches ≥ 85% | vitest coverage 閾値 |
| N-03 | **モデル重みをリポジトリにも Vercel にも置かない。** ONNX は Hugging Face の CDN から直接取得する | `public/` と `out/` に `.onnx` / `.bin` / `.safetensors` が存在しないことをテストで検査 |
| N-04 | 音声と認識結果をネットワークへ送らない | 認識経路のコードに `fetch`/`XMLHttpRequest`/`WebSocket` の送信呼び出しが無いことを検査(モデル取得は専用モジュールに隔離し、そこだけ許可) |
| N-05 | ランタイム依存は next / react / react-dom / @huggingface/transformers のみ | `package.json` の dependencies を検査 |
| N-06 | **循環の禁止。** 参照テキスト(およびその派生)が認識器へ渡る経路を持たない | G-03 |
| N-07 | 課題文は青空文庫の**著作権の消滅した**作品に限る。出典を作品 ID まで保持する | G-07 |
| N-08 | **成果物に大きな binary を載せない。** 実行系(ONNX Runtime の wasm)も重みと同じ扱いにし、版を固定した CDN から取る。成果物の総量は 3 MB 以下 | `scripts/check_bundle.mjs`(build のあとに走る) |

### N-03 / N-08 の理由

Vercel の無料枠は帯域が有限で、Whisper base の量子化重みは数十 MB ある。
これを `public/` に置くと**アクセス数百回で無料枠を焼く**。
「コストゼロ」が崩れる経路はここなので、重みの所在を非機能要求として固定する。

**loop_002 の実測で、同じ経路がもう一本あることが分かった。** webpack は
onnxruntime-web が持つ `new URL(....wasm)` を見て、23.6 MB の実行系を
`out/_next/static/media/` へ複製していた。成果物 25.0 MB のうち 94% がこれである。

厄介なのは壊れ方で、**ソースを読んでも分からず、閲覧しても気づけない** —
取りに行くのは `wasmPaths` の側だけなので転送量にも現れない。
N-03 のゲートは `.onnx` / `.bin` / `.safetensors` しか見ておらず、素通しした。

実行系は重みと同じ種類の物として扱う。取得先と**版**をこちらで固定し(HC-050)、
成果物には載せない。載っていないことと、**取得先を確かに名指ししていること**を
対で検査する — 後者が無いと「何も読み込まない」壊れ方が緑をすり抜ける(HC-052)。

実測 2026-08-29: この対処で成果物は 25.0 MB → 1.41 MB。

## 4. 品質基準(ゲート)

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 二実装照合(編集距離) | TS の `align()` と Python の独立実装が、乱択 1,000 ケースで**編集距離・操作内訳ともに完全一致** |
| G-02 | 合成復元 | 参照へ既知の操作列(置換/挿入/削除)を注入して作った転写に対し、検出された編集距離が**注入コスト以下**(DP の最小性)であり、かつ注入コストと単調に増える |
| G-03 | 循環の禁止 | `Recognizer` の入力型に参照テキスト型が現れないことを型と静的検査で固定。加えて、認識呼び出しに渡る値が課題文カタログ由来でないことを実行時アサートで検査 |
| G-04 | 陽性対照 | 置換率 p ∈ {0, 0.1, 0.3, 0.5, 1.0} を注入した合成転写で、厳密 CER が p と ±0.02 で一致する |
| G-05 | 位置写像の健全性 | 正規化の写像 `origIndex` が (a) 単調非減少 (b) すべて原文の有効範囲内 (c) 正規化が恒等な入力では恒等写像 |
| G-06 | リサンプラ | 48 kHz の既知正弦波(440 Hz / 1 kHz / 3 kHz)を 16 kHz に落として DFT ピークが ±1 bin で保存。ナイキスト(8 kHz)超の 10 kHz 入力が **−30 dB 以上減衰**する(折り返さない) |
| G-07 | カタログ健全性 | 全課題文が (a) `kana` が新字新仮名 (b) 正規化後に非空 (c) 本文が重複しない (d) 出典 4 項目が埋まっている (e) 文字数が既定域内 |
| G-08 | 文字種検査 | 日本語本文・カタログ・辞書にキリル文字およびギリシャ文字が混入しない |
| G-09 | 表記ゆれ辞書の健全性 | 各エントリが恒等でなく、値が別のキーにならず(連鎖しない)、正規形が冪等。加えて、カタログ全件で**表記だけを変えた転写の許容 CER が 0**、その課題文の厳密 CER が > 0 |
| G-10 | 決定論 | 同一入力に対し正規化・アライメント・計量の出力が完全一致(JSON 等値)。`Date.now()`/`Math.random()` を core で呼ばない |
| G-11 | モデルレジストリの健全性 | 各エントリが id・ラベル・採用理由・**実測バイト数**(実行系ごと)を持つ。捨てたモデルを理由と実測値つきで残す。WASM 側が WebGPU 側より軽い |
| G-12 | 重みの型の決定論 | 実行系から重みの型への写像が固定。WebGPU でもエンコーダは fp32(fp16 の既知の精度問題を避ける) |
| G-13 | 実行系の取得先 | 版が範囲指定でない。`mjs` と `wasm` を両方明示。取得先が自オリジンでない。Safari 判定が Chrome/Edge を巻き込まない |
| G-14 | 成果物の検査 | 1 MB 超の資産が理由つき一覧に収まり、総量が上限内で、`.onnx`/`.wasm` が載っていない。**かつ**、意図した取得先(CDN・Hugging Face・既定モデル)を成果物が名指ししている |

ゲートの数値は較正実験の証拠付きでのみ変更できる(緩和は人間の承認が必要)。

### 表記ゆれをどう扱うか(この企画の較正の核心)

認識結果と原文が字面で違っても、**読みが同じなら聞き取れている**。
「三十三」対「33」を誤りに数えると、機械の失敗ではなく表記の選択を測ってしまう。
かといって何でも同一視すると採点が甘くなり、指標が動かなくなる。

そこで指標を二本立てにする:

- **厳密 CER** — 正規化後の字面で測る。下限として動かさない
- **表記ゆれ許容 CER** — F-06 の決定論的な辞書で説明できる置換だけを一致に数え直す

辞書は**手で作り、エントリごとに出所を書く**。統計から自動生成しない
(自動生成すると「よく間違える対」を許容表に取り込み、指標が自分を甘くする方向へ動く)。

**分母は両者とも「厳密側の参照長」で揃える。** 正規形の長さで割ると、
表記の統一が参照を伸縮させた分だけ比が動き、二つの指標が比較できなくなる。
分母は「読むよう求めた文字数」である。

#### loop_001 の実測で捨てたもの

SPEC 初稿は「変換を両側へ等しく掛けるのだから、許容側が厳しくなることはない」
と書いていた。**これはカタログ実データで否定された**(24 件 × 置換率 3 段のうち
7/72 件で許容側が上回った)。機構は二つある。

1. **繰り返し記号の展開が誤りを増幅する。** `々` は直前の文字を写すので、
   その文字が誤っていると誤りが 2 文字に増える(「人々」対「ゐ々」で
   厳密 0.25 / 許容 0.50)。これは変換として正しく、指標の性質として受け入れる
2. **漢数字の桁並び解釈が読みを変えていた。** 初稿は位取りを含まない連なりを
   桁の並びとみなしたが(「一九四五」→ 1945)、日本語ではこれが成り立たない —
   実測で `千島`(ちしま)→ 1000島、`五六寸`(ごろくすん)→ 56寸、`三つ`(みっつ)→ 3つ。
   カタログ 24 件のうち 18 件が漢数字を含み、**単字の漢数字はほとんど数として
   読まれていない**(一切・一口・一向・百姓・九州・十分の一)

2 は辞書の収録基準そのものへの違反だったので、規則を
**「位取りを含み、かつ 2 文字以上の連なりだけを解く」**に絞った。
年号の「一九二七」は解けなくなったが、**解けないことと間違えることは違う**。
それでも残る誤り(八百屋・五十嵐など)は例外表に列挙し、
**その表が網羅でないこと**を明示する。

したがって「許容 CER ≤ 厳密 CER」はゲートにしない。二本の数は**同じ分母を持つ
別々の見方**であって、片方が他方の上界ではない。

### 実ブラウザでしか出なかったこと(loop_003 実測)

**`decoder_model_merged` の 8 bit 系が読み込めない。** ONNX Runtime 1.26.0-dev の
QDQ 最適化が落ちる:

```
qdq_actions.cc:137 TransposeDQWeightsForMatMulNBits
Missing required scale: model.decoder.embed_tokens.weight_merged_0_scale
```

実測(whisper-tiny / wasm)では q8・int8・uint8 が失敗し、fp16・bnb4・q4・fp32 が成功した。
エンコーダの q8 は通るので、デコーダ側だけの問題である。通るもののうち最小の **fp16** を採る。
初回に落ちる量は base で 73 MB → **122 MB** に増えた。これは実行系の都合であって、
選びたくて選んだ値ではない。

**単体テストもビルドも成果物検査も緑のまま**この故障を通していた。
verify の緑は「動くことの証明ではない」— この一件がその実例である。

なお、最初の測定では全 dtype が同じ失敗を示した。**一度落ちた ONNX Runtime の状態が
頁に残る**ためで、候補ごとにブラウザ文脈を作り直して初めて切り分けられた。
`scripts/probe_dtype.mjs` はそのために文脈を毎回捨てる。

### この検品が言えないこと

**TTS は人の音読の代わりにならない。** 合成音声は明瞭で間も一定なので、
ここで出る CER は人が読んだときの値ではない。さらに SAPI が漢字を読み違えれば、
Whisper が忠実に写しても誤りとして数えられる(TTS の読み誤りと ASR の誤りが混ざる)。

実測 2026-08-29(whisper-base / wasm / Haruka の 19.3 秒): 厳密 CER 45.5% / 表記ゆれ許容 38.4%。
**これは品質の数字ではなく、経路が通っていることの証拠**として読む。

## 5. スコープ外

- **話者分離・複数話者。** 単一話者の音読のみを対象にする
- **発音/アクセントの採点。** 音素・アクセント核の正誤には参照読みの権威データが要り、
  このプロジェクトにはオラクルが立たない。字面の一致までで止める
- **音声データの保存・送信・アップロード。** 録音はメモリ上に留め、永続化しない
- **サーバ側推論。** 実行時課金ゼロの原則に反する
- **kotoba-whisper の採用。** `onnx-community/kotoba-whisper-v2.2-ONNX` は実在し
  transformers.js 対応だが、large-v3 蒸留のためエンコーダが大きい。最小の組み合わせ
  (q4f16)でも **534 MB**(実測 2026-08-29)あり、初回に落とさせる量ではない。
  日本語特化の精度は望ましいので、より小さい蒸留版が出たら測り直す
- 旧仮名遣いの作品を課題文にすること(音読の正解が一意に定まらない)
