import Utsushi from "@/components/Utsushi";
import { PASSAGES } from "@/core/catalog";

// 日本語の本文は**文字列式で書く**。JSX の中で改行すると、その改行が
// 半角空白に畳まれて本文に入る(「原文の上に描く。 推論は」)。
// 語間に空白を置かない日本語では、これがそのまま目に付く。
// loop_003 の実ブラウザ検品(スクリーンショット)で見つけた。

export default function Page() {
  return (
    <main>
      <h1>音写ラボ</h1>
      <p className="tagline">
        {"青空文庫の一節を声に出して読み、機械が聞き落とす場所を原文の上に描く。推論はブラウザの中で完結し、音声も文字もどこへも送らない。"}
      </p>

      <p className="note">
        {"音声認識のアプリはたいてい「認識結果」を出して終わる。その結果が正しいかは誰も知らない。ここでは順序を逆にした —— "}
        <strong>{"正解が先に画面に在り"}</strong>
        {"、それを読み上げる。測るのは人の滑舌ではなく、"}
        <strong>{"機械の側の癖"}</strong>
        {"である。"}
      </p>

      <Utsushi />

      <h2>この道具の限りについて</h2>
      <p className="lede">
        {`課題文は ${PASSAGES.length} 篇。認識は端末の中だけで走り、音声も認識結果もどこへも送らない。`}
      </p>
      <p className="lede">
        {"ここで出る数は"}
        <strong>{"機械の癖の目録であって、読み手の採点ではない"}</strong>
        {"。同じ人が同じように読んでも、模型を変えれば数は動く。低い方がよい成績、という読み方をしないこと。"}
      </p>
    </main>
  );
}
