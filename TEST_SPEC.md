# TEST_SPEC.md — oto-utsushi(音写ラボ)

## 実行規約

- TypeScript コア: `npx vitest run`(stage 3–5 の判定に使用)
- Python(選定パイプライン・二実装照合の相手側): `python -m pytest -x -q`
- 二実装照合(G-01)は **TS 側から Python を起動して突き合わせる**のではなく、
  共有のケース JSON を Python が生成 → TS がそれを読んで自分の結果と比較する。
  片方が片方を呼ぶ形にすると、呼ばれた側の失敗が呼んだ側の失敗として現れて帰属が壊れる
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(最小性・単調性・可逆性等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない** |
| 外部権威(青空文庫の本文・公表値) | 作品 ID と取得元パスをフィクスチャ先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| 編集距離の期待値 | Python の独立実装(`tools/align_ref.py`)。標準ライブラリのみで書き、TS を参照しない | 二実装照合。**同じ疑似コードを二言語に写すのは照合ではない** — Python 側は行優先の全表 DP、TS 側は 2 行ローリング + 操作列復元で、実装戦略を意図的に変える |
| 合成転写 | 参照テキストへ既知の操作列を注入して生成(シード固定) | 陽性対照。注入率が指標に現れることを確かめる |
| 課題文の本文 | `aozora-sakuin/data/normalized/{id}.txt`(青空文庫・著作権消滅作品) | 外部権威。**本文をテストへ手で写さない**(写経は typo が正解になる) |
| リサンプラの期待値 | 閉形式(正弦波の周波数は変わらない・ナイキスト超は通さない) | 解析解 |
| 表記ゆれ辞書 | 手で作り、エントリごとに出所を書く | **統計から自動生成しない**(指標が自分を甘くする) |

## ケース一覧

### F-02 正規化(位置写像)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-02 / G-05 | 恒等な入力(かなのみ) | 出力が入力と一致し、`origIndex` が恒等 |
| T-002 | F-02 / G-05 | 全角英数・半角カナ混じり | NFKC 相当に畳まれ、`origIndex` が単調非減少 |
| T-003 | F-02 / G-05 | 句読点・括弧・ダッシュ・空白の除去 | 除去され、残った文字の `origIndex` が原文の実位置を指す |
| T-004 | F-02 | 空文字・記号のみ | 出力が空文字。例外を投げない |
| T-005 | F-02 / G-10 | 同一入力を 2 回 | 出力が JSON 等値 |

### F-03 アライメント

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | F-03 | 完全一致 | 距離 0、操作がすべて `equal` |
| T-011 | F-03 | 片方が空 | 距離 = もう片方の長さ、操作が全 `insert` または全 `delete` |
| T-012 | F-03 / G-01 | 乱択 1,000 ケース | 距離・操作内訳が Python 独立実装と**完全一致** |
| T-013 | F-03 | 操作列の整合 | 操作列を参照へ適用すると転写が復元される(操作列が距離と辻褄が合う) |
| T-014 | F-03 | 原文位置への逆写像 | 各操作が指す原文インデックスが有効範囲内で、`equal` の位置で原文と転写の文字が一致 |

### F-04 計量

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-020 | F-04 / G-04 | 置換率 p = 0 / 0.1 / 0.3 / 0.5 / 1.0 の合成転写 | 厳密 CER が p と ±0.02 で一致 |
| T-021 | F-04 / G-02 | 既知の操作列を注入 | 検出距離 ≤ 注入コスト、かつ注入コストに対し単調非減少 |
| T-022 | F-04 | 読み飛ばし | 連続削除 ≥ k を 1 件として検出。k 未満は検出しない |
| T-023 | F-04 | 言い直し | 挿入直後に同一部分列が再開する形を検出。単なる挿入と区別する |
| T-024 | F-04 | 表記ゆれ許容 CER | 辞書で説明できる置換のみ一致に数え直す。分母は厳密側の参照長 |
| T-024b | F-04 / G-09 | カタログ全件・表記だけを変えた転写 | 許容 CER が 0、厳密 CER が > 0。転写側の表記ゆれは**テスト内で独立に作る**(core の変換を使って作ると循環する) |
| T-024c | F-04 | 許容 CER が厳密 CER を上回る既知の形 | 「人々」対「ゐ々」で厳密 0.25 / 許容 0.50。上界関係は成り立たないことを検査で固定する |
| T-025 | F-04 | 読速 | 文字数 / 秒。継続時間 0 で例外を投げず null を返す |

### F-05 リサンプラ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | F-05 / G-06 | 48 kHz の 440 Hz / 1 kHz / 3 kHz 正弦波 | 16 kHz へ変換後の DFT ピークが元の周波数と ±1 bin |
| T-031 | F-05 / G-06 | 48 kHz の 10 kHz 正弦波(ナイキスト超) | 16 kHz 出力での帯域内エネルギーが −30 dB 以上減衰(折り返さない) |
| T-032 | F-05 | ステレオ入力 | 2 ch が平均されてモノラルになる |
| T-033 | F-05 | 既に 16 kHz | 変換が恒等(サンプル列が一致) |
| T-034 | F-05 | 長さ 0 の入力 | 長さ 0 を返し、例外を投げない |

### F-01 / F-06 カタログと辞書

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-040 | F-01 / G-07 | 全課題文 | `kana` が新字新仮名・正規化後非空・本文重複なし・出典 4 項目が充足・文字数域内 |
| T-041 | F-01 | 本文の実在 | 各課題文が出典作品の当該オフセットの本文と**バイト単位で一致**する(写経していないことの検査) |
| T-042 | F-06 / G-09 | 辞書の各エントリ | 恒等でない・循環しない・適用が合流的 |
| T-043 | G-08 | カタログ・辞書・UI 文言 | キリル文字/ギリシャ文字が混入しない |

### N-03 / N-04 / N-06 構成の検査

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-050 | N-03 | `public/` と `out/` | `.onnx` / `.bin` / `.safetensors` が存在しない |
| T-051 | N-04 | 認識経路のソース | モデル取得モジュール以外に送信呼び出しが無い |
| T-052 | N-06 / G-03 | `Recognizer` の入力型 | 参照テキスト型(`ReferenceText`)を含まない。認識呼び出しに課題文由来の値が渡らない |
| T-053 | N-01 | ビルド構成 | `app/api` が存在せず `output: "export"` が設定されている |
| T-054 | N-05 | `package.json` | dependencies が許可リストと一致 |
| T-055 | N-08 / G-14 | `out/` の現物(build のあと) | 1 MB 超の資産が理由つき一覧に収まる・総量 3 MB 以下・`.onnx`/`.wasm` が無い |
| T-056 | N-08 / G-14 | `out/` の現物 | 取得先(jsDelivr の固定版・huggingface.co・既定モデル id)を**名指ししている**。T-055 と対で置く — これが無いと「何も読み込まない」壊れ方が緑をすり抜ける |

### F-07 モデルと実行系

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-060 | F-07 / G-11 | レジストリ全件 | id・ラベル・採用理由・実測バイト数が揃い、WASM 側が WebGPU 側より軽い |
| T-061 | F-07 / G-11 | 捨てたモデル | 理由に「実測」を含み、採用したどのモデルより重い。採用一覧に残っていない |
| T-062 | F-07 / G-12 | `dtypeFor` | 実行系 → 重みの型が固定。WebGPU のエンコーダは fp32 |
| T-063 | F-07 / G-13 | `wasmPaths` | 版が範囲指定でない・mjs と wasm が対・取得先が自オリジンでない |
| T-064 | F-07 / G-13 | Safari 判定 | Chrome / Edge / Firefox の UA を Safari と誤らない |
| T-065 | F-07 | 単スレッド退避の表示 | `crossOriginIsolated` でない WASM を「単スレッド」と表示する(黙らせない) |

### 機械では確かめられないこと

**推論そのものは実ブラウザでしか確かめられない。** 型・境界・取得先・成果物は
このテスト群が縛るが、「実際に日本語を聞き取るか」「WebGPU が使われるか」
「Safari で実行系が落ちてくるか」はここでは分からない。
人がブラウザで読み上げて確かめる段を、公開前の受け入れ条件として別に置く。
